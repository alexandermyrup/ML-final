# Core
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
scikit-learn>=1.4
joblib>=1.3

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Notebook
jupyter>=1.0
ipykernel>=6.0

# Optional but recommended (preserved from submission)
lightgbm>=4.0     # used by Scripts 03 and 06 if installed; gracefully skipped otherwise
optuna>=3.5       # required only by Script 06 (tuning); cell no-ops if absent
shap>=0.44        # used by Script 04 for top-1% SHAP analysis; gracefully skipped otherwise
